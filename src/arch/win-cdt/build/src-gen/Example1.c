/** Generated by YAKINDU Statechart Tools code generator. */

#include "Example1.h"
#include "..\src\sc_types.h"

/*! \file Implementation of the state machine 'example1'
*/

/* prototypes of all internal functions */
static void enseq_main_region_S1_default(Example1* handle);
static void enseq_main_region_default(Example1* handle);
static void exseq_main_region_S1(Example1* handle);
static void exseq_main_region(Example1* handle);
static void react_main_region__entry_Default(Example1* handle);
static sc_boolean react(Example1* handle);
static sc_boolean main_region_S1_react(Example1* handle, const sc_boolean try_transition);
static void clearInEvents(Example1* handle);
static void clearOutEvents(Example1* handle);


void example1_init(Example1* handle)
{
	sc_integer i;
	
	for (i = 0; i < EXAMPLE1_MAX_ORTHOGONAL_STATES; ++i)
	{
		handle->stateConfVector[i] = Example1_last_state;
	}
	
	
	handle->stateConfVectorPosition = 0;
	
	clearInEvents(handle);
	clearOutEvents(handle);
	
}

void example1_enter(Example1* handle)
{
	/* Default enter sequence for statechart example1 */
	enseq_main_region_default(handle);
}

void example1_runCycle(Example1* handle)
{
	clearOutEvents(handle);
	for (handle->stateConfVectorPosition = 0;
		handle->stateConfVectorPosition < EXAMPLE1_MAX_ORTHOGONAL_STATES;
		handle->stateConfVectorPosition++)
		{
			
		switch (handle->stateConfVector[handle->stateConfVectorPosition])
		{
		case Example1_main_region_S1:
		{
			main_region_S1_react(handle, bool_true);
			break;
		}
		default:
			break;
		}
	}
	
	clearInEvents(handle);
}

void example1_exit(Example1* handle)
{
	/* Default exit sequence for statechart example1 */
	exseq_main_region(handle);
}

sc_boolean example1_isActive(const Example1* handle)
{
	sc_boolean result = bool_false;
	sc_integer i;
	
	for(i = 0; i < EXAMPLE1_MAX_ORTHOGONAL_STATES; i++)
	{
		result = result || handle->stateConfVector[i] != Example1_last_state;
	}
	
	return result;
}

/* 
 * Always returns 'false' since this state machine can never become final.
 */
sc_boolean example1_isFinal(const Example1* handle)
{
   return bool_false;
}

sc_boolean example1_isStateActive(const Example1* handle, Example1States state)
{
	sc_boolean result = bool_false;
	switch (state)
	{
		case Example1_main_region_S1 :
			result = (sc_boolean) (handle->stateConfVector[SCVI_EXAMPLE1_MAIN_REGION_S1] == Example1_main_region_S1
			);
			break;
		default:
			result = bool_false;
			break;
	}
	return result;
}

static void clearInEvents(Example1* handle)
{
}

static void clearOutEvents(Example1* handle)
{
}




/* implementations of all internal functions */

/* 'default' enter sequence for state S1 */
static void enseq_main_region_S1_default(Example1* handle)
{
	/* 'default' enter sequence for state S1 */
	handle->stateConfVector[0] = Example1_main_region_S1;
	handle->stateConfVectorPosition = 0;
}

/* 'default' enter sequence for region main region */
static void enseq_main_region_default(Example1* handle)
{
	/* 'default' enter sequence for region main region */
	react_main_region__entry_Default(handle);
}

/* Default exit sequence for state S1 */
static void exseq_main_region_S1(Example1* handle)
{
	/* Default exit sequence for state S1 */
	handle->stateConfVector[0] = Example1_last_state;
	handle->stateConfVectorPosition = 0;
}

/* Default exit sequence for region main region */
static void exseq_main_region(Example1* handle)
{
	/* Default exit sequence for region main region */
	/* Handle exit of all possible states (of example1.main_region) at position 0... */
	switch(handle->stateConfVector[ 0 ])
	{
		case Example1_main_region_S1 :
		{
			exseq_main_region_S1(handle);
			break;
		}
		default: break;
	}
}

/* Default react sequence for initial entry  */
static void react_main_region__entry_Default(Example1* handle)
{
	/* Default react sequence for initial entry  */
	enseq_main_region_S1_default(handle);
}

static sc_boolean react(Example1* handle) {
	/* State machine reactions. */
	return bool_false;
}

static sc_boolean main_region_S1_react(Example1* handle, const sc_boolean try_transition) {
	/* The reactions of state S1. */
	sc_boolean did_transition = try_transition;
	if (try_transition == bool_true)
	{ 
		if ((react(handle)) == (bool_false))
		{ 
			did_transition = bool_false;
		} 
	} 
	return did_transition;
}


